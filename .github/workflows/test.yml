name: Test Action
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test action
        uses: pretoriustheuns/updaterepovar@a14d63d8f3d979824a332ddaac3cf014964f65e7
        with:
          variable: MASTER_BUILDNOTES_LAST_COMMITSHA
          newvalue: testingfromaction12345
          owner: Signify-Software
          repo: Signify
          token: ${{ secrets.REPO_TOKEN }}
