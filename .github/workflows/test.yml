name: Test Action
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test action
        uses: pretoriustheuns/updaterepovar@a3ad19c4d073962941c1a9d934c8b734cdd8a5c4
        with:
          variable: MASTER_BUILDNOTES_LAST_COMMITSHA
          newvalue: testingfromaction12345
          owner: Signify-Software
          repo: Signify
          token: ${{ secrets.REPO_TOKEN }}
